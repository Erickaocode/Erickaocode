name: Update streak stats

on:
  schedule:
    - cron: "0 3 * * *" # Roda todo dia às 03:00 UTC
  push:
    paths:
      - ".github/workflows/streak-stats.yml" # Roda sempre que este arquivo for modificado
  workflow_dispatch: # Permite rodar manualmente na aba "Actions"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Generate streak stats
        uses: DenverCoder1/github-readme-streak-stats@main
        with:
          options: user=Erickaocode&background=000000&hide_border=true&disable_animations=true&sideLabels=1F6FEB&sideNums=1F6FEB&currStreakLabel=1F6FEB&currStreakNum=1F6FEB&fire=FFA500&ring=1F6FEB&dates=8b949e&stroke=1F6FEB
          path: profile/streak.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit streak stats
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/streak.svg
          git commit -m "Update streak stats" || exit 0
          git push
